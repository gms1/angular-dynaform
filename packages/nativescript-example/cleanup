#!/bin/bash
DN=$(dirname "$0")

pushd "${DN}" &>/dev/null

  rm -rf node_modules/
  rm -f package-lock.json
  rm -rf platforms/*
  rm -rf hooks/
  
  npm link '@angular-dynaform/core'
  npm link '@angular-dynaform/nativescript'

  npm install

  tns prepare android

popd

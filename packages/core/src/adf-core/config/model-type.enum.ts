export const MODEL_GROUP = 'form:group';    // based on Angular FormGroup
export const MODEL_SUBSET = 'form:subset';  // a subset of the parents FormGroup(MODEL_GROUP) items

export const MODEL_ARRAY = 'form:array';  // based on Angular FormArray (all array items are using the same model)

export const MODEL_VALUE = 'form:value';  // based an Angular FormControl
export const MODEL_NULL = 'form:null';    // a FormControl having always a undefined value ( e.g push buttons )
